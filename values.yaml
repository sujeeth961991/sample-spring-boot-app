# Default values for delphichart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: delphiacr.azurecr.io/sujeethsamplespringbootapp
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080